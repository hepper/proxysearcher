<UserControl x:Class="ProxySearch.Console.Controls.BandwidthSettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:toolkit="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:converters="clr-namespace:ProxySearch.Console.Code.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="300" Name="BandwidthSettings">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="130" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        
        <Label Grid.Column="0">Max Bandwidth</Label>
        <toolkit:DoubleUpDown Grid.Column="1" Name="MaxBandwidthUpDown" TextAlignment="Left" Value="{Binding ElementName=BandwidthSettings, Path=MaxBandwidth}" Minimum="0.1" Increment="0.1" />
        <Label Grid.Column="2">Mb/s</Label>
    </Grid>
</UserControl>
