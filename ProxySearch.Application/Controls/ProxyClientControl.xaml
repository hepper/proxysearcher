<UserControl x:Class="ProxySearch.Console.Controls.ProxyClientControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Name="Control">
    <CheckBox IsChecked="{Binding ElementName=Control, Path=IsChecked}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Image Grid.Column="0" Source="{Binding ElementName=Control, Path=ProxyClient.Image}" />
            <Label Grid.Column="1"  Content="{Binding ElementName=Control, Path=ProxyClient.Name}"/>
        </Grid>
    </CheckBox>
</UserControl>
