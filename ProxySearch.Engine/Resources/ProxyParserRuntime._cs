using System.Net;
using System.Text.RegularExpressions;
using ProxySearch.Engine.Proxies;

namespace ProxySearch.Engine.Resources
{{
    public class ProxyParserRuntime
    {{
        public Proxy GetProxyOrNull(Match match)
        {{
            try
            {{
                 {0}
            }}
            catch
            {{
                return null;
            }}
        }}
    }}
}}
